name: Supabase Keep Alive

on:
  schedule:
    # 매주 월, 목요일 자정(UTC 기준)에 실행
    - cron: '0 0 * * *'
  workflow_dispatch: # 수동 실행 허용

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send Request to Supabase API
        run: |
          curl -X GET "https://oanttaxtqsxaszbdqxbq.supabase.co/rest/v1/" \
          -H "apikey: sb_publishable_aMbi-onE5wnYrqKuDzRhZg_g0SXhhdv" \
          -H "Authorization: Bearer sb_publishable_aMbi-onE5wnYrqKuDzRhZg_g0SXhhdv"
